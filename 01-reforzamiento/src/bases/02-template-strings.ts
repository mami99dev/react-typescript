const firstName = 'Isaac'
const lastName = "O'Neal"

const fullName = `${firstName} ${lastName}` // Al usar las template strings, se pueden incluir variables dentro de la cadena de texto utilizando la sintaxis ${variable}. En este caso, se crea una nueva cadena de texto que combina el valor de firstName y lastName con un espacio entre ellos.

console.log({firstName, lastName, fullName})